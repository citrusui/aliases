language: bash
sudo: required
addons:
  apt:
    packages: shellcheck
os:
 - linux
 - osx
script:
 - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then shellcheck init.sh; fi
 - ./init.sh -y
 - if [ "$(type -t update)" != "function" ]; then exit 1; fi
notifications:
  email: false
